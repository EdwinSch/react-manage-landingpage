import Button from "../Button/Button";

const Incentive = () => {
  return (
    <section className="incentive-container">
      <p>Simplify how your team works today.</p>
      <Button variant={"btn-invert"} />
    </section>
  );
};

export default Incentive;
